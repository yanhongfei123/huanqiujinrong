import record from './record';
import userInfo from './userInfo';


const personal = {
    namespaced: true,
    actions: {

    },
    modules: {
        record,
        userInfo,
    },
};

export default personal;